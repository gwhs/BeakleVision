pre-commit:
  commands:
    ruff-check:
      glob: "server/*.py"
      stage_fixed: true
      run: ruff check {staged_files} --fix --exit-non-zero-on-fix
    ruff-fmt:
      glob: "server/*.py"
      stage_fixed: true
      run: ruff format {staged_files}
